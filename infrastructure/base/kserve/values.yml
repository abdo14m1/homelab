kserve:
  controller:
    deploymentMode: Standard
    gateway:
      domain: farag.tech
      kserveGateway: kube-system/main-gateway
      domainTemplate: '{{ .Name }}-{{ .Namespace }}.{{ .IngressDomain }}'
      urlScheme: https
      ingressGateway:
        enableGatewayApi: true
        className: cilium
        createGateway: false